import React from 'react';
import './style.css';

const Header = () => {

    return (
        <header id="header" className="pin-top">
            <h1 className="title">
                Header
            </h1>
        </header>
    );
};

export default Header;