import React from 'react';
import Chat from '../components/ChatRoom';

const ChatRoom = () => {
    return (
        <main id="Chat">
            <Chat />
        </main>
    );
};

export default ChatRoom;