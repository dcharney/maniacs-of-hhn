import React from 'react';
import FanContent from '../components/FanContent';

const FanContent = () => {
    return (
        <main>
            <FanContent />
        </main>
    );
};

export default FanContent;